#include "tree.h"
#include <string>
#include <iostream>
#include <cassert>
using namespace std;

int main()
{
	cout << "Hello World";
	/*
	WordTree k;
	k.add("Kim");
	k.add("Kanye");
	k.add("Kanye");
	k.add("Kanye");
	cout << k;
	if (k.distinctWords() == 2 && k.totalWords() == 4)
		cout << "All tests passed\n" << endl;
	WordTree j = k;
	j.add("Khloe");
	j.add("Kendall");
	cout << j << endl;
	WordTree w;
	w.add("Harry");
	w.add("Niall");
	w.add("Niall");
	w.add("Liam");
	w.add("Louis");
	w.add("Harry");
	w.add("Niall");
	w.add("Zayn");
	cout << w << endl;
	j = w;
	cout << j << endl;

	WordTree b;
	WordTree a;
	b = a;
	if (b.totalWords() == 0 && b.distinctWords() == 0 && a.totalWords() == 0 && b.totalWords() == 0)
		cout << "Tree A and B are both empty. Assignment operator on two empty trees --> passed.\n";
	WordTree c;
	c.add("Brooke");
	WordTree d;
	d = c;
	int test = d.totalWords();
	cout << "D's total words are " << test << endl;
	if (d.totalWords() == 1)
		cout << "Tree D went from empty to 1 item. Assigning an empty tree to a non-empty tree --> passed\n";
	c = a;
	if (c.totalWords() == 0)
		cout << "Tree C is now empty. Assigning a non-empty tree to an empty tree --> passed.\n";
	WordTree e = d;
	cout << "E's total words are " << e.totalWords() << endl;

	WordTree f;
	f.add("Kershaw");
	f.add("Kershaw");
	f.add("Bellinger");
	WordTree g;
	g.add("Bumgarner");

	cout << "Before the swap: \n" << f << endl;
	cout << g << endl;
	cout << "F's total words are " << f.totalWords() << " and distinct words are " << f.distinctWords() << endl;
	cout << "G's total words are " << g.totalWords() << " and distinct words are " << g.distinctWords() << endl;
	WordTree temp = f;
	f = g;
	g = temp;
	f.add("Bumgarner");
	cout << "After the swap: \n" << f << endl << g << endl;
	cout << "F's total words are " << f.totalWords() << " and distinct words are " << f.distinctWords() << endl;
	cout << "G's total words are " << g.totalWords() << " and distinct words are " << g.distinctWords() << endl;
	*/
}